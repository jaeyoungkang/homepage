---
title: "에이전트에게 규칙을 가르치는 가장 확실한 방법"
date: 2026-04-12
tags: ["배움", "계산+LLM"]
draft: true
translationKey: teaching-rules-to-agents
---

API 경로 상수 12개, 에러 ���탈로그 17개, i18n 메시지 맵 709개 키. 이걸 한 곳에 모으고 67개 파일을 마이그레이션하는 데 꽤 시간이 ���렸다.

왜 이 작업을 했냐면, AI 제품을 만들면서 코드베이스가 커지니까 같은 값이 여러 파일에 흩어지는 문제가 반복됐다. 에이전트가 새 코드를 쓸 때 기존 상수를 참조하지 않고 직접 값을 넣�� 경우도 있었다. 문서로 규칙을 정해두는 것만으로는 부족했다.

그래서 선언-���핑-강제라는 3단 패턴을 만들었다. 값을 한 곳에 ���언하고, 사용 지점에서 그걸 참조하게 매핑하고, 우회를 기계적으로 차단한다. 마지막 강제 단계가 핵심이다. ESLint no-restricted-syntax 규칙 35��을 추가해서 API 경로 리터럴과 한글 throw를 빌드에서 에러로 ���았다. 이게 agentic-base의 9번째 품질 ���이트가 됐다.

값의 원본이 한 곳이 되자 "어디에 추가���지?"라는 질문이 사라졌다. 이건 에이전트��� 아니라 사람에게도 마찬가지다. 강제 단계가 없으면 선언과 매핑은 그냥 약속에 머문다. 빌드가 깨져야 규칙이 된다.

한편으로 이 작업은 계산+LLM 결합의 다른 면이기도 하다. LLM이 코드를 생성하고, 기계적 도구(린터, 테스트)가 그 코드의 품질을 보정한다. 둘의 결합이 AI 제품 개발에서 반복적으로 나타나는 패턴이다.
